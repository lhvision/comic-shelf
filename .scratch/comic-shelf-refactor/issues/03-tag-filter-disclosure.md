# 03 — 标签筛选墙 → 「更多标签」渐进展开

**What to build:** 书架标签筛选条不再一次显示最多 18 个标签 chip；默认只显示
6–8 个高频标签 + 「更多标签」展开按钮，展开后展示其余（可再次收起），移动端
不再成为超长换行条。

**Blocked by:** None — 可以立即开始（只动 `TagFilterBar` 与传入的 tagCounts）。

**Status:** ready-for-agent

- [ ] 默认渲染 ≤8 个高频标签，其余收进「更多标签」展开
- [ ] 展开/收起状态可访问（`aria-expanded`），不丢「全部」与「只看喜欢」入口
- [ ] 移动端 390px 下无横向溢出、无超长换行，`vp check` 通过

## 相关评审上下文

critique 29/40 Priority P2：~20 个 chip 同时可见，超出工作记忆上限；移动端
成为超长换行条。
